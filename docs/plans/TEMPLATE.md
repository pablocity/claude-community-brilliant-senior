# <what this change is>

Written by `/plan`. Reviewed by `plan-reviewer` before anything gets built.

## Context

Why this is being done, and what the user ends up seeing that they did not see before.
One paragraph. If you cannot say what changes for the user, the plan is not ready.

## Files to touch

| Path | What changes there |
|---|---|
| `src/...` | one line, concrete |

## Interfaces

The actual types, quoted from the code with a `file:line` reference. Not paraphrased,
not remembered. If it is not quoted, it is not verified.

## Test cases

Numbered, input and expected result, written **before** any implementation exists.

1. given ..., when ..., then ...
2. given ..., when ..., then ...

## Acceptance

The exact command that proves it, and what its output looks like when it passes.

## Out of scope

- <thing>, because <reason>
- <thing>, because <reason>
