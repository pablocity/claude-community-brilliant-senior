# State

A snapshot of where this project is, not a log. Entries that stop being true get deleted,
not annotated. Written by `/wrap`, read by `/catchup`.

Last updated: <date>

## In flight

What is half done right now. Delete each entry when it lands or dies.

- CHANGE: replace this with your own. One bullet per thing actually in progress.

## Open questions

What you do not know yet, phrased as a question with the decision it blocks.

- CHANGE: replace this with your own.

## Recently shipped

The last ten or so. Older ones are in git, which is better at this than a markdown file.

- CHANGE: replace this with your own.

## Scars

Dead ends, with the reason. This is the section that pays for the file. Without it,
tomorrow's session walks confidently into yesterday's dead end, on your tokens, because
it will look just as reasonable the second time.

Never pruned for length. The reason matters more than the conclusion: "do not do X" gets
re-litigated, "do not do X because Y" does not.

- CHANGE: replace this with your own. Real examples of the shape:
  - that API returns 401 to apps rather than users. Do not try again.
  - the CLI needs a tsconfig in the root. It looks redundant. It is not.
